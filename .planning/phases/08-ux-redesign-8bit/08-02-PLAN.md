---
phase: 08-ux-redesign-8bit
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - GoProStitcher/Features/Home/HomeView.swift
  - GoProStitcher/Features/FolderPicker/FolderPickerView.swift
  - GoProStitcher/Features/ChunkReview/ChunkReviewView.swift
  - GoProStitcher/Features/ChunkReview/ChunkPreviewModal.swift
  - GoProStitcher/Features/StitchProgress/StitchProgressView.swift
  - GoProStitcher/Features/AudioExtraction/AudioExtractionView.swift
  - GoProStitcher/ContentView.swift
  - GoProStitcher/GoProStitcherApp.swift
autonomous: false

must_haves:
  truths:
    - "Every screen uses RetroColor.beigeBackground as its root background — no system background colors"
    - "Every text element uses RetroFont.bold() or RetroFont.regular() — no .font(.title), .font(.headline), or system fonts"
    - "No RoundedRectangle, no .shadow(), no .blur(), no gradients appear in any restyled view"
    - "StitchProgressView and AudioExtractionView use RetroProgressBar instead of ProgressView"
    - "HomeView has a retro game menu feel — bold monospace title, tool rows styled as RetroCard, chevron replaced with retro arrow or text indicator"
    - "ChunkReviewView rows have 2px black borders and monospace text, with 4pt grid padding"
    - "All buttons use RetroButton or RetroButtonStyle"
    - "ContentView and GoProStitcherApp set beige as window background"
  artifacts:
    - path: "GoProStitcher/Features/Home/HomeView.swift"
      provides: "Retro game menu home screen"
    - path: "GoProStitcher/Features/FolderPicker/FolderPickerView.swift"
      provides: "Beige bg, centered pill RetroButton, monospace text"
    - path: "GoProStitcher/Features/ChunkReview/ChunkReviewView.swift"
      provides: "Bordered rows, monospace, 4pt grid chunk list"
    - path: "GoProStitcher/Features/StitchProgress/StitchProgressView.swift"
      provides: "RetroProgressBar in RetroCard, pixel checkmark text"
    - path: "GoProStitcher/Features/AudioExtraction/AudioExtractionView.swift"
      provides: "RetroInvertedCard metadata header, RetroProgressBar extraction progress"
    - path: "GoProStitcher/ContentView.swift"
      provides: "Beige root background applied to entire app"
    - path: "GoProStitcher/GoProStitcherApp.swift"
      provides: "Window background set to beige"
  key_links:
    - from: "every restyled view"
      to: "GoProStitcher/DesignSystem/DesignTokens.swift"
      via: "RetroColor.*, RetroFont.*, RetroSpacing.* references"
      pattern: "RetroColor\\.|RetroFont\\.|RetroSpacing\\."
    - from: "StitchProgressView.swift + AudioExtractionView.swift"
      to: "GoProStitcher/DesignSystem/RetroProgressBar.swift"
      via: "RetroProgressBar(fraction:) replaces ProgressView"
      pattern: "RetroProgressBar"
---

<objective>
Restyle every screen to match the 8-bit design system. HomeView gets Claude's creative retro game menu treatment. All other screens apply design tokens mechanically per the spec from 08-CONTEXT.md.

Purpose: Transform the visual language from macOS default to GoProToolkit-8bit-system without changing any TCA reducer logic or navigation wiring.
Output: All 8 view files restyled. Zero system UI styling remaining. Build passes. Checkpoint for visual evaluation.
</objective>

<execution_context>
@/Users/rishizamvar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rishizamvar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rishizamvar/Documents/Projects/GoProStitcher/.planning/phases/08-ux-redesign-8bit/08-CONTEXT.md
@/Users/rishizamvar/Documents/Projects/GoProStitcher/.planning/phases/08-ux-redesign-8bit/08-01-SUMMARY.md
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/DesignSystem/DesignTokens.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/DesignSystem/RetroProgressBar.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/DesignSystem/RetroButton.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/DesignSystem/RetroCard.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/Home/HomeView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/FolderPicker/FolderPickerView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/ChunkReview/ChunkReviewView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/ChunkReview/ChunkPreviewModal.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/StitchProgress/StitchProgressView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/Features/AudioExtraction/AudioExtractionView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/ContentView.swift
@/Users/rishizamvar/Documents/Projects/GoProStitcher/GoProStitcher/GoProStitcherApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle HomeView, FolderPickerView, ContentView, GoProStitcherApp</name>
  <files>
    GoProStitcher/Features/Home/HomeView.swift
    GoProStitcher/Features/FolderPicker/FolderPickerView.swift
    GoProStitcher/ContentView.swift
    GoProStitcher/GoProStitcherApp.swift
  </files>
  <action>
    Apply design tokens to the four container/entry-point files. Preserve all TCA store wiring unchanged.

    **GoProStitcherApp.swift:** Add `.background(RetroColor.beigeBackground)` to the WindowGroup's root view, or set the NSWindow background via a SwiftUI window modifier. Use:
    ```swift
    .background(RetroColor.beigeBackground.ignoresSafeArea())
    ```

    **ContentView.swift:** Wrap the root ZStack/switch in `.background(RetroColor.beigeBackground.ignoresSafeArea())`. Replace the "< Back" button text with `RetroButton(title: "< BACK") { store.send(.backToHome) }`. Remove the manual `.padding(.horizontal, 16)` wrapper and use `RetroSpacing.md` instead.

    **FolderPickerView.swift:**
    - Root background: `RetroColor.beigeBackground`
    - Title "GoPro Toolkit": `RetroFont.bold(24)`, `RetroColor.black`
    - Subtitle: `RetroFont.regular(13)`, `RetroColor.muted`
    - "Select Folder" button: `RetroButton(title: store.isLoading ? "SCANNING..." : "SELECT FOLDER") { store.send(.selectFolderButtonTapped) }`, centered horizontally, not full-width
    - Remove `.buttonStyle(.borderedProminent)`
    - Loading state: replace ProgressView with `RetroProgressBar(fraction: 0.5, blockCount: 8)` as a placeholder spinner — or simply show animated dots using `Text("...")` in monospace
    - Result cards: wrap in `RetroCard { ... }` — remove `.green.opacity(0.08)` and `RoundedRectangle` backgrounds. Success state: black checkmark text "✓" in `RetroFont.bold`, `RetroColor.black`. Error state: "✗" in `RetroColor.accentRed`. No SF Symbols.
    - Spacing: all padding values replaced with `RetroSpacing.*` constants
    - Remove `.animation(.easeInOut...)` — replace with `.animation(.linear(duration: 0.1), ...)`

    **HomeView.swift — Retro Game Menu (Claude's creative freedom):**
    Design as a retro game select screen. Suggestions:
    - Black header bar at top (RetroInvertedCard) showing "GoPro Toolkit" in large bold monospace
    - Below: each ToolDescriptor rendered as a RetroCard with:
      - Bold ALL-CAPS tool title on left in `RetroFont.bold(16)`
      - Subtitle in `RetroFont.regular(12)`, `RetroColor.muted`
      - Right side: "[ SELECT ]" or "> START" styled text in `RetroFont.bold(11)`, `RetroColor.black`
      - Hover state: card background flips from RetroColor.white to RetroColor.black, text inverts to RetroColor.beigeBackground
    - Whole card is the tap target (`.contentShape(Rectangle())`)
    - 4pt grid spacing throughout
    - Keep the `tools: [ToolDescriptor]` array pattern unchanged — extensibility preserved
    - No system images (SF Symbols removed or replaced with ASCII art like "▶ " prefix on title)
    - Frame: minWidth 400, minHeight 300, background RetroColor.beigeBackground
  </action>
  <verify>
    xcodebuild build -scheme GoProStitcher -destination "platform=macOS" 2>&1 | tail -5
    BUILD SUCCEEDED — no errors related to these files.
  </verify>
  <done>
    HomeView, FolderPickerView, ContentView, GoProStitcherApp all compile with zero errors. No system fonts or backgrounds remain in these files. All text uses RetroFont.*, all colors use RetroColor.*.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle ChunkReviewView, ChunkPreviewModal, StitchProgressView, AudioExtractionView</name>
  <files>
    GoProStitcher/Features/ChunkReview/ChunkReviewView.swift
    GoProStitcher/Features/ChunkReview/ChunkPreviewModal.swift
    GoProStitcher/Features/StitchProgress/StitchProgressView.swift
    GoProStitcher/Features/AudioExtraction/AudioExtractionView.swift
  </files>
  <action>
    Apply design tokens to the four feature view files. Preserve all TCA reducer wiring.

    **ChunkReviewView.swift:**
    - Root background: `RetroColor.beigeBackground`
    - Header: title "REVIEW CLIPS" in `RetroFont.bold(18)`, subtitle in `RetroFont.regular(12)` `RetroColor.muted`
    - "Start Stitching" button: replace `.buttonStyle(.borderedProminent)` with `RetroButton` or `.buttonStyle(RetroButtonStyle())`
    - Remove `Divider()` — replace with a 2px `Rectangle().fill(RetroColor.black).frame(height: 2)`
    - ChunkRowView: remove `RoundedRectangle` clip on thumbnail; use `Rectangle()` clip (hard edge). Add `RetroSpacing.xs` padding around each row. Row separator: 1px `RetroColor.beigeSecondary` line.
    - Filename text: already `.system(.body, design: .monospaced)` — replace with `RetroFont.body`
    - Metadata string: `RetroFont.caption`, `RetroColor.muted`
    - List background: `.scrollContentBackground(.hidden)` + `.background(RetroColor.beigeBackground)` on the List
    - All padding values: replace with `RetroSpacing.*` constants (16 -> `.md`, 12 -> `.sm`, 4 -> `.xs`)

    **ChunkPreviewModal.swift:** Read the file first to understand its structure. Apply:
    - Background: `RetroColor.black` (modal as dark overlay)
    - Any text: `RetroFont.*`, `RetroColor.beigeBackground` (inverted on dark bg)
    - Close/done button: `RetroButton` with inverted colors (beige on black)

    **StitchProgressView.swift:**
    - Root background: `RetroColor.beigeBackground`
    - Title: `RetroFont.bold(18)`, `RetroColor.black`
    - Replace `ProgressView(value: progressValue).progressViewStyle(.linear)` with:
      `RetroProgressBar(fraction: progressValue, blockCount: 16)`
    - Phase label: `RetroFont.regular(13)`, `RetroColor.muted`
    - Completion state: remove `Image(systemName: "checkmark.circle.fill")` — replace with `Text("████ DONE ████")` or `Text("[✓ COMPLETE]")` in `RetroFont.bold(16)`, `RetroColor.black`. Put in a `RetroCard`.
    - Error state: remove system xmark image — replace with `Text("[✗ ERROR]")` in `RetroFont.bold(16)`, `RetroColor.accentRed`. Error message text in `RetroFont.regular(12)`, `RetroColor.accentRed`.
    - Wrap active state (not complete, not error) in a `RetroCard { ... }` with `RetroSpacing.md` padding
    - All spacing: `RetroSpacing.*`

    **AudioExtractionView.swift (AudioFilePickerView + AudioExtractionView):**

    AudioFilePickerView:
    - Background: `RetroColor.beigeBackground`
    - Title: `RetroFont.bold(18)`, `RetroColor.black`
    - Subtitle: `RetroFont.regular(13)`, `RetroColor.muted`
    - Button: `RetroButton(title: "SELECT MP4 FILE") { store.send(.selectFileButtonTapped) }`, centered, no Label/SF Symbol
    - Loading: remove `ProgressView()` — show `RetroProgressBar(fraction: 0.0, blockCount: 8)` or `Text("[ LOADING... ]")` in `RetroFont.regular(12)`

    AudioExtractionView:
    - Title: `RetroFont.bold(18)`, `RetroColor.black`, background `RetroColor.beigeBackground`
    - Metadata card: use `RetroInvertedCard { ... }` for the header bar containing the filename, then `RetroCard { ... }` for the body rows (Duration, Size, Audio Bitrate). Label column: `RetroFont.bold(11)`, `RetroColor.beigeSecondary` (on inverted header) or `RetroColor.muted` (on white card). Value column: `RetroFont.regular(13)`, `RetroColor.beigeBackground` (inverted) or `RetroColor.black` (card).
    - Replace `ProgressView(value:)` with `RetroProgressBar(fraction: store.progressFraction, blockCount: 16)`
    - Replace indeterminate `ProgressView()` with `RetroProgressBar(fraction: 0.0, blockCount: 8)` + "STARTING..." text
    - Completion state: `Text("[✓ MP3 SAVED]")` in `RetroFont.bold(16)`, `RetroColor.black`. Output filename in `RetroFont.regular(12)`, `RetroColor.muted`.
    - Error state: `Text("[✗ FAILED]")` in `RetroFont.bold(16)`, `RetroColor.accentRed`
    - Remove all `.cornerRadius()`, `.background(Color(nsColor: .windowBackgroundColor))`, SF Symbol images
    - Remove `metadataRow` function — inline into the card structure
    - All padding: `RetroSpacing.*`
  </action>
  <verify>
    xcodebuild build -scheme GoProStitcher -destination "platform=macOS" 2>&1 | tail -5
    BUILD SUCCEEDED — no errors related to these files.
    Also run: grep -r "ProgressView\|RoundedRectangle\|\.shadow\|\.blur\|easeInOut\|easeIn\|easeOut\|regularMaterial\|windowBackgroundColor\|fontWeight(.bold\|font(.title\|font(.headline\|font(.subheadline\|font(.caption\|font(.body\|font(.largeTitle\|font(.footnote" GoProStitcher/Features/ GoProStitcher/ContentView.swift GoProStitcher/GoProStitcherApp.swift
    This grep should return zero results (all system styling removed).
  </verify>
  <done>
    All 4 feature files compile. The grep for system styling tokens returns zero matches. No ProgressView, no RoundedRectangle, no shadow/blur, no system font modifiers in any restyled file.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 8 view files restyled to 8-bit design system: beige backgrounds, hard black borders, JetBrains Mono throughout, RetroProgressBar in progress views, retro game menu HomeView, no system shadows or rounded corners anywhere.
  </what-built>
  <how-to-verify>
    1. Build and run the app: Product > Run in Xcode (or `xcodebuild run`)
    2. Home screen: should feel like a retro game menu — bold monospace title, tool rows as cards with hard borders
    3. Click "Stitch Video": FolderPickerView appears — beige background, centered button, monospace text
    4. Select any folder: result card has hard border, no rounded corners, monospace text
    5. If GoPro files detected, advance to ChunkReviewView — rows should have black borders, monospace filenames
    6. Start stitching: StitchProgressView shows block-fill progress bar (████░░░░), no system ProgressView
    7. Return to home, click "Extract Audio": AudioFilePickerView appears — centered RetroButton, beige bg
    8. Select any MP4: AudioExtractionView shows inverted metadata header card, block progress bar
    9. Check: zero rounded corners anywhere, zero gradients, zero shadows, all text in JetBrains Mono
    10. Take screenshots of each screen for the polish evaluation in 08-03
  </how-to-verify>
  <resume-signal>Type "approved" if the 8-bit visual language is applied correctly, or describe specific screens that need fixes.</resume-signal>
</task>

</tasks>

<verification>
xcodebuild build -scheme GoProStitcher -destination "platform=macOS" 2>&1 | tail -5
BUILD SUCCEEDED. All 8 view files use DesignSystem tokens exclusively.
</verification>

<success_criteria>
- All 8 view files compile and the app runs end-to-end without crashes
- Zero system ProgressView, RoundedRectangle, shadow, blur in restyled files
- Zero system font modifiers (.title, .headline, .body etc.) in restyled files — all replaced with RetroFont.*
- RetroProgressBar visible in StitchProgressView and AudioExtractionView
- HomeView has retro game menu aesthetic
- Human verifies visual result and types "approved" or specific feedback
</success_criteria>

<output>
After completion and checkpoint approval, create `.planning/phases/08-ux-redesign-8bit/08-02-SUMMARY.md`
</output>
